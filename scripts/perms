#!/usr/bin/env bash

if [[ $# -eq 0 ]]; then
    echo "Usage: $0 <file1> <file2> ..."
    exit 1
fi

for file in "$@"; do
    [[ -e "$file" ]] || { echo "File not found: $file"; continue; }
    perms=$(stat -c "%a" "$file")
    echo "$perms $file"
done


